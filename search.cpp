#include "chord.h"
#include "node.h"
#include "finger.h"

string find_successor(ll id){
	predecessor_id=find_predecessor(id);
	successor_id=get_successor_of(predecessor_id);
}

void find_predecessor(ll id){

}
